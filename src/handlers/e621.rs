use super::HandlerImpl;

pub struct E621Handler;

impl E621Handler {
    pub fn new() -> Self {
        Self
    }
}

impl HandlerImpl for E621Handler {
    fn handle() -> Result<(), HandlerError> {
        
    }
}